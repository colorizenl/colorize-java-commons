name: Build
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    container: colorizenl/colorize-ci
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Unit tests
        run: gradle coverage
      - name: Copyright header check
        run: copyright_check.py
